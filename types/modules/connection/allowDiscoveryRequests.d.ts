import { C_Connection } from "@dogma-project/constants-meta";
import ConnectionClass from "../connections";
export default function allowDiscoveryRequests(this: ConnectionClass, direction: C_Connection.Direction): boolean;
